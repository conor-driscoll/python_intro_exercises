# This code will print the following output:
# Product2
# Product not found!

# This is because '113' matches a specific case, but 142 doesn't.
# 142 doesn't, even though its digits do match a specific case, because the type doesn't match.